function setup() {
    createCanvas(100, 100);
}

function draw() {
    background(255, 48, 67); //Change background
    fill(155, 0, 18); //fill the inside of the circle
    noStroke(); //we don't want the border
    circle(50, 50, 30);
}